services:
  mongo:
    image: mongo:7
    container_name: weg-mongo
    restart: unless-stopped
    volumes:
      - mongo_data:/data/db
    networks:
      - internal
    # MongoDB is NOT exposed to the host — internal only

  fastapi-app:
    build:
      context: ./backend_api
    container_name: weg-api
    restart: unless-stopped
    env_file:
      - ./backend_api/.env
    environment:
      - MONGODB_URL=mongodb://weg-mongo:27017
    ports:
      - "8835:8000"
    volumes:
      - photo_data:/data/photos
      - manual_data:/data/manuals
    depends_on:
      - mongo
    networks:
      - internal

  admin-frontend:
    build:
      context: ./backend_admin_page
      args:
        - VITE_API_BASE_URL=${VITE_API_BASE_URL:-https://api.west_end_glass.customertest.digitalbullet.net}
    container_name: weg-frontend
    restart: unless-stopped
    ports:
      - "3030:3000"
    networks:
      - internal

volumes:
  mongo_data:
  photo_data:
  manual_data:

networks:
  internal:
    driver: bridge
